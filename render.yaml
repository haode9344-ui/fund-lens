services:
  - type: web
    name: fund-lens
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    autoDeploy: true
    envVars:
      - key: FUND_LENS_ALLOWED_ORIGINS
        value: https://haode9344-ui.github.io
